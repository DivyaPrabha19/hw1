#include<stdio.h>
#include<math.h>
int square(int,int);
int main()
{
    {
    int a,b,k;
    printf("Enter the numbers:");
    scanf("%d %d ",&a,&b);
    k=square(a,b);
    printf("%d",k);
    
}
int square(int a,int b)
{
    return (pow(a,b));
}
}
